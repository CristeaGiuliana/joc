function setup() {
    createCanvas(600, 600);
}

function draw() {
    background(220);
    fill(255, 255, 255); // Culoarea alb
    ellipse(200, 200, 50); // Desenează o bilă la coordonatele (200, 200) cu diametrul 50 
    ellipse(260, 200, 50);
    ellipse(320, 200, 50);
    ellipse(380, 200, 50);
    fill(0, 0, 0);//culoarea negru
    ellipse(140, 100, 50);

}
